{
  "name": "searchstax-crawler-outcome-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic QA fixture site for crawler operational outcome testing.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
